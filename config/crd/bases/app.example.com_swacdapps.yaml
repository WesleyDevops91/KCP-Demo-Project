---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: swacdapps.app.example.com
spec:
  group: app.example.com
  names:
    kind: SwacdAPP
    listKind: SwacdAPPList
    plural: swacdapps
    singular: swacdapp
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SwacdAPP is the Schema for the swacdapps API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SwacdAPPSpec defines the desired state of SwacdAPP.
            properties:
              foo:
                description: Foo is an example field of SwacdAPP. Edit swacdapp_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: SwacdAPPStatus defines the observed state of SwacdAPP.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
